import BookForm from "../components/BookForm";

export default {
  title: "Componentes/BookForm",
  component: BookForm,
};

const Template = () => <BookForm />;

export const Default = Template.bind({});
